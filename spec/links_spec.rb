require "spec_helper"

RSpec.describe JekyllAbsoluteLinks do
  it "has a version number" do
    expect(Jekyll::Absolute::Links::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
